# 如何把代码上传/更新到服务器

你有两种方式，任选一种即可。

---

## 方式一：用 Git（推荐，适合已经会用 GitHub 的情况）

**思路：** 本机改完代码 → 推到 GitHub → 服务器上拉取。

### 在你自己的电脑（Cursor 里改代码的这台）

1. 改完代码后，在终端执行：
   ```bash
   cd /Users/fanye/Documents/stock-analysis-bot
   git add .
   git commit -m "你的修改说明"
   git push origin main
   ```
2. 如果从没配置过 Git 推送，需要先登录 GitHub（或配置 SSH key），按提示做一次即可。

### 在服务器上（先 SSH 登录，再执行下面命令）

3. **先在本机登录服务器**（用密码或密钥）：
   ```bash
   ssh root@154.17.3.182
   ```
4. **在服务器上**进入项目并拉取最新代码：
   ```bash
   cd ~/stock-analysis-bot
   git pull origin main
   ```
5. **在服务器上**运行机器人：
   ```bash
   ./venv/bin/python github_stock_bot.py --mode manual --stocks '688630'
   ```

注意：`~/stock-analysis-bot` 和 `./venv/bin/python` 是**服务器上**的路径；你本机项目在 `Documents/stock-analysis-bot`，虚拟环境叫 `.venv`，不要在本机执行 `cd ~/stock-analysis-bot`。

**优点：** 有提交历史，代码在 GitHub 有备份；服务器上一条 `git pull` 就更新。  
**缺点：** 需要会一点 Git（add / commit / push）。

---

## 方式二：用脚本从本机直接上传（不用 Git）

**思路：** 在你自己的电脑上运行一个脚本，把当前项目文件夹整份传到服务器，覆盖服务器上的代码（不删服务器上的 `.env` 和 `reports/`）。

### 第一次：在你自己电脑上准备好

1. 打开终端（Terminal），进入项目目录：
   ```bash
   cd /Users/fanye/Documents/stock-analysis-bot
   ```
2. 确认你有 SSH 私钥（之前用密钥登录过服务器的话就有），例如：
   ```bash
   ls /Users/fanye/Documents/ssh_rsa_keys/private_key/id_rsa.pem
   ```
   如果路径不对，把你实际放私钥的路径记下来，后面脚本里要改成你的路径。

### 每次要更新服务器代码时

3. 在本机执行上传脚本（见下一节「上传脚本」）：
   ```bash
   cd /Users/fanye/Documents/stock-analysis-bot
   bash 上传到服务器.sh
   ```
4. 脚本会把你当前电脑上的 `stock-analysis-bot` 文件夹同步到服务器的 `~/stock-analysis-bot`，**不会覆盖**服务器上已有的 `.env` 和 `reports/`。
5. 上传完成后，**再 SSH 进服务器**运行机器人：
   ```bash
   ssh root@154.17.3.182
   cd ~/stock-analysis-bot
   ./venv/bin/python github_stock_bot.py --mode manual --stocks '688630'
   ```

**优点：** 不用学 Git，本机改完就跑一个脚本即可。  
**缺点：** 没有提交历史，备份要靠你自己（例如本机或 U 盘）。

---

## Telegram 报告机器人（发代码收报告）

在 Telegram 里向你的 Bot 发送股票代码（如 `688630` 或 `688630 300474`），机器人生成报告并把 PDF 发回同一对话。

### 环境变量（与现有推送一致）

在服务器或本机的 `.env` 中配置：

- `TELEGRAM_BOT_TOKEN`：Bot Token（与现有推送用同一个即可）
- `TELEGRAM_CHAT_ID`：你的对话 ID；**只有该对话发来的消息会触发生成**，其他人发消息会收到「未授权」提示

可选：`TELEGRAM_REPORT_ALLOW_CHAT_IDS`，逗号分隔的多个 chat_id，允许这些人触发生成；不设则仅允许 `TELEGRAM_CHAT_ID`。

### 启动方式（需长期运行）

在服务器或本机执行（虚拟环境按你实际路径来）：

```bash
cd ~/stock-analysis-bot
./venv/bin/python telegram_report_bot.py
```

或本机：

```bash
cd /Users/fanye/Documents/stock-analysis-bot
python telegram_report_bot.py
```

进程会一直运行，等待 Telegram 消息。单条消息最多处理 5 只股票；收到消息后先回复「正在生成…」，生成完成后逐个发送 PDF 并发送完成或失败汇总。用 `Ctrl+C` 可退出。

建议在服务器上用 `screen` 或 `systemd` 常驻运行，例如：

```bash
screen -S telegram_bot
cd ~/stock-analysis-bot && ./venv/bin/python telegram_report_bot.py
# 按 Ctrl+A 再按 D 脱离 screen；重新连接：screen -r telegram_bot
```

---

## 上传脚本（方式二专用）

把下面整段保存成项目里的 `上传到服务器.sh`，然后在本机执行 `bash 上传到服务器.sh` 即可上传。

脚本里需要改的地方只有两处（如果和你环境一致就不用改）：

- `PRIVATE_KEY`：改成你本机 SSH 私钥的路径（例如 `id_rsa.pem` 的路径）。
- `SERVER_IP`：改成你的服务器 IP（你的是 `154.17.3.182`）。

脚本已保存在项目根目录：**`上传到服务器.sh`**。  
若你的私钥路径或服务器 IP 不同，用编辑器打开该文件，修改顶部的 `PRIVATE_KEY` 和 `SERVER_IP` 即可。
