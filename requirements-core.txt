# 核心依赖 - 运行程序必需（兼容 Python 3.9+）
# 使用范围版本便于 pip 选择有预编译 wheel 的版本，避免从源码编译失败
# 数据处理
pandas>=2.0,<3
numpy>=1.24,<3

# 数据获取
requests>=2.28,<2.33

# 数据可视化
matplotlib>=3.7,<3.11
seaborn>=0.12,<0.14

# PDF生成
reportlab>=4.0,<5
Pillow>=10,<12

# 配置管理
PyYAML>=6.0,<7
python-dotenv>=1.0,<2

# 日志
loguru>=0.7,<0.8
